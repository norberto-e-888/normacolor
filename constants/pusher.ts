export enum PusherEventName {
  NewMessage = "new-message",
  NewNotification = "new-notification",
}

export enum PusherChannelType {
  OrderChat = "private-order-chat",
  Notifications = "private-notifications",
}
